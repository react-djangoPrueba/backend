from django.test import TestCase
"""
from .models import Users
from django.contrib.auth import get_user_model

User = get_user_model()

# Create your tests here.
class MyTestCase(TestCase):

    def setUp(self):
        user = User.objects.create_user(username='Hola',email='hola2@hola.com',password='abc123')
        self.users = Users.objects.create(name='Hola',text='hola2',is_active=True,father=user)
        #definir el entorno de la prueba

    def test_user(self):
        self.assertTrue(self.users.is_active)
"""
